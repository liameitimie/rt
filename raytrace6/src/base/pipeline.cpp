#include <pipeline.h>
#include <scene.h>
#include <shape.h>

using namespace std;

// void process_shapes(Scene* scene){
//     vector<Vertex> vertices;
//     vector<int> indices;

//     for(auto shape: scene->shapes){
//         int sz=vertices.size();
//         for(auto vertex: shape->vertices()) vertices.push_back(vertex);
//         for(auto index: shape->indices()) indices.push_back(index+sz);
//     }
// }

Pipeline* Pipeline::create(Scene* scene){
    
    return nullptr;
}